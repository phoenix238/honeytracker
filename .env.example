# Copy to .env.local for `npm run dev`. Only the first two are needed locally.
APP_PASSWORD=
SESSION_SECRET=
# DATABASE_URL=            # leave unset locally to use PGlite in .data/
# STARLING_TOKEN=
# ANTHROPIC_API_KEY=
# CSTL_URL=
# CSTL_FINANCE_TOKEN=
# CRON_SECRET=
